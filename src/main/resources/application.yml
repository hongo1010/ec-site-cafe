spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/ec-site-cafe
    username: postgres
    password: postgres
server:
  servlet:
    context-path: /ec-site-cafe
